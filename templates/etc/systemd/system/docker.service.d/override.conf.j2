# {{ ansible_managed }}

[Service]
ExecStart=
ExecStart=/usr/bin/dockerd --tlsverify --tlscacert={{ docker_cert_path }}/ca_cert.pem --tlscert={{ docker_cert_path }}/server_cert.pem --tlskey={{ docker_cert_path }}/server_key.pem -H unix:///var/run/docker.sock -H tcp://0.0.0.0:2376
